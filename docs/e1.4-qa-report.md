# E1.4 — Property Type Inference — QA Report

## Header

| Field | Value |
|---|---|
| Epic | E1.4 |
| Name | Property Type Inference |
| Milestone | M1 — Catalog & Naming |
| Date | 2026-06-07 |
| Branch | `epic/e1.4-property-type-inference` |
| Environment | macOS Darwin 25.4.0, rustc 1.96.0, cargo 1.96.0 |
| Overall success rate | **11/11 (100%)** |
| Verdict | **GREEN** |

## Summary

**11/11 cases passed (100%) · GREEN · 0 N/A**

All E1.4 deliverables verified. PII detection, type heuristics, param extraction, and JSX input extraction work end-to-end. The NextjsAdapter now enriches all event properties with PII flags and inferred types. 302 workspace tests pass.

## Feature Results Table

| Feature | What it proves | Result | Success Rate | Notes |
|---------|---------------|--------|-------------|-------|
| F1: `is_pii_property` | PII detection across camelCase/snake_case/PascalCase/kebab identifiers | PASS | 100% | 9 unit tests; "email", "password", "full_name", "userEmail" etc. all detected |
| F2: `type_from_name` | Heuristic boolean and number inference from property name patterns | PASS | 100% | 10 unit tests; is_*/has_* → boolean, count/total/amount → number, unknown → None |
| F3: `enrich_hints` | Fills missing types, sets PII flags, preserves existing type_hint, idempotent | PASS | 100% | 5 unit tests + 1 integration test |
| F4: `hints_from_params` — typed | TS primitive type annotations extracted from function params | PASS | 100% | 3 unit tests (string, number, boolean) |
| F5: `hints_from_params` — untyped/destructured | Untyped params get type_hint:None; destructured params skipped | PASS | 100% | 3 unit tests |
| F6: `hints_from_jsx_inputs` | `<input>`, `<select>`, `<textarea>` name attrs extracted from JSX | PASS | 100% | 11 unit tests covering nesting, dedup, dynamic names, no-JSX case |
| F7: NextjsAdapter — param extraction | Form submit handlers produce typed + PII-flagged property hints | PASS | 100% | 4 integration tests |
| F8: NextjsAdapter — JSX input extraction | Form input field names appear as properties on FormSubmit events | PASS | 100% | 2 integration tests |
| F9: Auth/API properties not PII | `method`, `endpoint`, `route` properties have `pii_hint=false` | PASS | 100% | 3 integration tests |
| F10: Type enrichment from name heuristics | `isRemember` param (no TS type) gets `type_hint:"boolean"` | PASS | 100% | 1 integration test |
| F11: Backward compat | All 248 pre-E1.4 workspace tests still pass | PASS | 100% | `cargo test` — 302/302 total (54 new) |

## Evidence

```
$ cargo test -p infergen-core property
cargo test: 45 passed, 237 filtered out (6 suites, 0.00s)

$ cargo test -p infergen-core --test property
cargo test: 15 passed (1 suite, 0.00s)

$ cargo test
cargo test: 302 passed (11 suites, 0.32s)

$ cargo clippy -p infergen-core
cargo clippy: No issues found
```

## N/A List

None. All E1.4 deliverables are testable without downstream dependencies.

## Recommendations

- E6.1 (Local LLM Integration) can now use `is_pii_property` and `type_from_name` as pre-filters — only send low-confidence properties to the LLM for refinement.
- E9.4 (Compliance / Manifest Service) depends on PII flags from this epic. The `pii: bool` field in `EventProperty` is now populated end-to-end for all events produced by the NextjsAdapter.
- Structural convention linting (E1.3 deferred feature: entity/action/state template) would benefit from property-name token lists established here — the `split_identifier` + `PII_TOKENS` pattern can be reused.
