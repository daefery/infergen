# E1.2 — Heuristic Event Namer — QA Report

## Header

| Field | Value |
|---|---|
| Epic | E1.2 |
| Name | Heuristic Event Namer |
| Milestone | M1 — Catalog & Naming |
| Date | 2026-06-07 |
| Branch | `epic/e1.2-heuristic-event-namer` |
| Commit | `f919cd4` |
| Environment | macOS Darwin 25.4.0, rustc 1.96.0, cargo 1.96.0 |
| Overall success rate | **12/12 (100%)** |
| Verdict | **GREEN** |

## Summary

**12/12 cases passed (100%) · GREEN · 0 N/A**

All E1.2 deliverables verified. The heuristic namer module is functional, produces correct snake_case names from all signal combinations, confidence ordering is correct, adapter attribution flows end-to-end to catalog provenance, and all 195 workspace tests pass.

## Feature Results Table

| Feature | What it proves | Result | Success Rate | Notes |
|---------|---------------|--------|-------------|-------|
| F1: Route-based naming | `Namer::derive` with route signal → correct name | PASS | 100% | "/about" → "about_page_viewed", "/" → "home_page_viewed" |
| F2: Handler-based naming | `Namer::derive` with handler signal (auth, submit, HTTP) | PASS | 100% | All auth variants, form handlers, HTTP methods correct |
| F3: Component-based naming | `Namer::derive` with PascalCase component | PASS | 100% | "CheckoutForm" → "checkout_submitted" |
| F4: `split_identifier` | camelCase/PascalCase/snake/kebab/ALLCAPS → tokens | PASS | 100% | 11 unit tests + 3 integration tests pass |
| F5: Confidence scoring | Route > component > handler > fallback ordering | PASS | 100% | Integration test confirms ordering holds |
| F6: `ProposedEvent.adapter` | Defaults to "", `with_adapter` sets value | PASS | 100% | 3 new unit tests pass |
| F7: NextjsAdapter attribution | All proposals carry `adapter="nextjs"` | PASS | 100% | 4-file integration test + unit test pass |
| F8: Catalog provenance propagation | `EventProvenance.adapter` populated from proposal | PASS | 100% | 2 catalog unit tests + 2 integration tests pass |
| F9: Determinism | Same inputs → same output on repeated calls | PASS | 100% | Dedicated determinism test passes |
| F10: snake_case output invariant | All namer output is valid snake_case | PASS | 100% | `^[a-z][a-z0-9_]*$` checked on 5 input patterns |
| F11: `route_to_name_prefix` moved | Public from `namer::`, unchanged behavior | PASS | 100% | Existing nextjs route tests pass via namer import |
| F12: Backward compat | All 195 pre-existing workspace tests still pass | PASS | 100% | `cargo test` — 195/195 |

## Evidence

```
$ cargo test 2>&1
cargo test: 195 passed (9 suites, 0.52s)
```

```
$ cargo clippy -p infergen-core
cargo clippy: No issues found
```

```
$ cargo test -p infergen-core namer
cargo test: 50 passed, 125 filtered out (4 suites, 0.00s)
```

```
$ cargo test -p infergen-core adapter
cargo test: 42 passed, 133 filtered out (4 suites, 0.02s)
```

```
$ cargo test -p infergen-core catalog
cargo test: 19 passed, 156 filtered out (4 suites, 0.00s)
```

## N/A List

None. All features are testable without downstream dependencies.

## Recommendations

- E1.3 (Naming Convention Engine & Linter) can now build directly on `Namer` and `NameSignals`. The `Namer` struct is designed as a ZST so a configurable convention wrapper can be introduced without breaking existing callers.
- E6.1 (Local LLM Integration) references E1.2 as its dependency — the `NameResult.confidence` field provides the low-confidence threshold for LLM refinement triggers.
