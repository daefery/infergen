# QA Report — E3.4: Consent & PII Controls

**Epic:** E3.4  
**Name:** Consent & PII Controls  
**Milestone:** 3 — Runtime SDK & Providers  
**Date:** 2026-06-07  
**Branch:** `epic/e3.4-consent-pii-controls`  
**Commit:** (pre-commit — QA run on working tree)  
**Environment:** Darwin 25.4.0, Rust 1.x (via ~/.cargo/bin/cargo)  
**Overall success rate:** 6/6 testable cases (100%)  
**Verdict:** GREEN

---

## Summary

**6/6 cases passed (100%) · GREEN · 0 N/A**

---

## Feature Results Table

| Feature | What it proves | Result | Success rate | Notes |
|---------|---------------|--------|-------------|-------|
| Consent gating (`setConsent` / `getConsent`) | `ConsentState` type, getter/setter, "denied" blocks dispatch | PASS | 100% | 4/4 assertions |
| Opt-out / Opt-in | Global opt-out blocks all dispatch; `optIn` re-enables | PASS | 100% | 3/3 assertions |
| Per-property redaction hooks | `RedactFn` type + `setRedactFn`; null drops property, non-null passes through | PASS | 100% | 4/4 assertions |
| Region routing | `setRegion`/`setRegionRoutes` filters providers by id for active region | PASS | 100% | 3/3 assertions |
| Central `_dispatch` integration | Generated track functions call `_dispatch`, not `_providers.forEach` | PASS | 100% | 3/3 assertions |
| `configureInfergen` consent init | `InfergenConfig` accepts `consent`/`region`/`redactFn`; called on init | PASS | 100% | 3/3 assertions |

---

## Feature Detail

### Feature 1 — Consent Gating

**Steps:**
```
cargo test -p infergen-core --lib codegen::consent::tests::consent_state_type_exported
cargo test -p infergen-core --lib codegen::consent::tests::consent_state_has_three_values
cargo test -p infergen-core --lib codegen::consent::tests::set_get_consent_exported
cargo test -p infergen-core --lib codegen::consent::tests::dispatch_blocks_on_denied
```

**Result:** PASS — 4/4 assertions passed.
- `export type ConsentState` present
- "granted", "denied", "unknown" all present
- `setConsent` and `getConsent` exported
- `_consentState === "denied"` gate in `_dispatch`

---

### Feature 2 — Opt-out / Opt-in

**Steps:**
```
cargo test -p infergen-core --lib codegen::consent::tests::opt_out_in_exported
cargo test -p infergen-core --lib codegen::consent::tests::is_opted_out_exported
cargo test -p infergen-core --lib codegen::consent::tests::dispatch_blocks_on_opted_out
```

**Result:** PASS — 3/3 assertions passed.

---

### Feature 3 — Per-property Redaction Hooks

**Steps:**
```
cargo test -p infergen-core --lib codegen::consent::tests::redact_fn_type_exported
cargo test -p infergen-core --lib codegen::consent::tests::set_redact_fn_exported
cargo test -p infergen-core --lib codegen::consent::tests::dispatch_applies_redact_fn
cargo test -p infergen-core --lib codegen::consent::tests::dispatch_drops_null_redacted
```

**Result:** PASS — 4/4 assertions passed.
- `RedactFn` type exported
- `setRedactFn` exported
- `_redactFn(key, value, eventName)` called in `_dispatch`
- `if (result !== null)` property-drop guard present

---

### Feature 4 — Region Routing

**Steps:**
```
cargo test -p infergen-core --lib codegen::consent::tests::set_get_region_exported
cargo test -p infergen-core --lib codegen::consent::tests::set_region_routes_exported
cargo test -p infergen-core --lib codegen::consent::tests::dispatch_region_routing
```

**Result:** PASS — 3/3 assertions passed.
- `setRegion`/`getRegion` exported
- `setRegionRoutes` exported
- `_regionRoutes[_region]` lookup + `allowed.includes(p.id)` filter in `_dispatch`

---

### Feature 5 — Central `_dispatch` Integration

**Steps:**
```
cargo test -p infergen-core --lib codegen::consent::tests::dispatch_function_present
cargo test -p infergen-core --lib codegen::consent::tests::dispatch_delegates_to_targets
cargo test -p infergen-core --lib codegen::tests::generate_track_fn_uses_dispatch
```

**Result:** PASS — 3/3 assertions passed.
- `export function _dispatch` present in consent module
- `targets.forEach(p => p.track(eventName` present
- Generated track functions use `_dispatch(`, no `_providers.forEach`

---

### Feature 6 — `configureInfergen` Consent Init

**Steps:**
```
cargo test -p infergen-core --lib codegen::tests::generate_infergen_config_has_consent_field
cargo test -p infergen-core --lib codegen::tests::generate_infergen_config_has_region_field
cargo test -p infergen-core --lib codegen::tests::generate_infergen_config_has_redact_fn_field
cargo test -p infergen-core --lib codegen::tests::generate_configure_infergen_calls_set_consent
cargo test -p infergen-core --lib codegen::tests::generate_configure_infergen_calls_set_region
cargo test -p infergen-core --lib codegen::tests::generate_configure_infergen_calls_set_redact_fn
```

**Result:** PASS — 6/6 sub-assertions in 3 combined cases (counted as 3 assertions in table).
- `consent?: ConsentState`, `region?: string`, `redactFn?: RedactFn` in `InfergenConfig`
- `setConsent(config.consent)`, `setRegion(config.region)`, `setRedactFn(config.redactFn)` called in `configureInfergen`

---

## N/A List

None. All E3.4 capabilities are self-contained within the codegen layer.

---

## Recommendations

None. All features pass. No regressions in existing 318 passing tests (337 total, 19 new consent tests).
