# E5.1 — QA Report

**Epic:** E5.1 — Python Parser & Adapters  
**Milestone:** M5 — Polyglot & Framework Breadth  
**Date:** 2026-06-08  
**Branch:** `epic/e5.1-python-parser-adapters`  
**Commit:** (pre-commit — working tree)  
**Environment:** macOS Darwin 25.4.0 · rustc 1.96.0 · cargo 1.96.0

**Overall success rate:** 16/16 testable cases passed (100%)  
**Verdict:** GREEN

**Summary:** 16/16 cases passed (100%) · GREEN · 0 N/A

---

## Feature Results

| Feature | What it proves | Result | Success Rate | Notes |
|---------|---------------|--------|-------------|-------|
| Python line-scanner | `PyParser::scan` extracts `FunctionDef`, `ClassDef`, `Decorator`, `ImportFrom`, `Import`, `Assign` from Python source | PASS | 100% | 20+ unit tests; integration tests in `tests/py_parser.rs` |
| `ParsedFile::with_py_ast` | Non-Python files return `R::default()`; Python files scan and call closure | PASS | 100% | 3 tests in `parser/mod.rs` |
| FastAPI adapter | Detects `@app.get/post/delete/put` routes → `ApiCall`; `@router.*` routes; OAuth2 import → `AuthEvent`; non-Python returns empty | PASS | 100% | 10 unit + 8 integration tests |
| Django adapter | Detects `urlpatterns` (single-line + multiline); CBV bases → correct `EventKind`; form fields as `PropertyHint`; PII flagging; auth imports in `urls.py` | PASS | 100% | 13 unit + 8 integration tests; multiline fix applied |
| Flask adapter | Detects `@app.route` (GET→PageView, POST→ApiCall); multi-method expansion; `login_user` import → `AuthEvent`; `errorhandler(N)` → `Error` | PASS | 100% | 12 unit + 8 integration tests |
| Python codegen | `generate_python` produces `TypedDict`, `track_*` functions, `_TrackNamespace`, `configure`, `_dispatch`, empty catalog → `EventName = None` | PASS | 100% | 22 unit + 8 integration tests |
| Adapter attribution | All proposals from each adapter carry correct `adapter` field (`"fastapi"`, `"django"`, `"flask"`) | PASS | 100% | Verified in unit + integration tests |
| PII propagation | `EmailField` / `PasswordInput` in Django forms; `email` prop on events → `pii_hint = true` | PASS | 100% | `email_field_flagged_pii` + integration test |
| Python codegen PII comment | PII props emit `# PII: handle with care` in TypedDict | PASS | 100% | `pii_prop_has_comment` + integration test |
| `scan.rs` dispatch | `collect_py_files` excludes `__pycache__`, `venv`, etc.; FastAPI/Django/Flask dispatch correct; Python files only scanned when `Language::Python` detected | PASS | 100% | Code review + build passes |
| `generate.rs` Python output | Python SDK written to `infergen_sdk.py` when `Language::Python` detected in cwd | PASS | 100% | Code review + build passes |
| Sorted alphabetical output | Events in generated Python SDK ordered A→Z | PASS | 100% | `sorted_alphabetically` unit + integration test |
| Deterministic output | Two calls with same catalog produce identical Python SDK | PASS | 100% | `deterministic` unit + integration test |
| No external Python dep | No `rustpython-parser` or other Python crate added; zero new Cargo dependencies | PASS | 100% | `Cargo.toml` unchanged |
| Full workspace compiles | `cargo build --workspace` 0 errors, 0 warnings after fixes | PASS | 100% | 2 warnings fixed (unused import + dead const) |
| Full workspace tests | `cargo test --workspace` 681 pass, 0 fail | PASS | 100% | Green |

---

## Failure Detail

None — all 16 features passed.

---

## N/A Features

None.

---

## Recommendations

- E5.2 (Go) and E5.3 (Ruby) can follow the same line-scanner pattern established here.
- Consider adding `detect_url_patterns_source` approach to FastAPI/Flask as well — currently they don't suffer the multiline issue (decorator + function are always on separate lines), but could benefit from resilience.
