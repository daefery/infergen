# E1.3 — Naming Convention Engine & Linter — QA Report

## Header

| Field | Value |
|---|---|
| Epic | E1.3 |
| Name | Naming Convention Engine & Linter |
| Milestone | M1 — Catalog & Naming |
| Date | 2026-06-07 |
| Branch | `epic/e1.3-naming-convention-engine-linter` |
| Environment | macOS Darwin 25.4.0, rustc 1.96.0, cargo 1.96.0 |
| Overall success rate | **13/13 (100%)** |
| Verdict | **GREEN** |

## Summary

**13/13 cases passed (100%) · GREEN · 0 N/A**

All E1.3 deliverables verified. `ConventionCase`, `LintRule`, `LintViolation`, `lint_catalog`, `apply_case`, `to_convention_name`, and `is_valid_case` are fully functional. Default snake_case linting works end-to-end from `NextjsAdapter` proposals through `Catalog` to `Vec<LintViolation>`. 248 workspace tests pass.

## Feature Results Table

| Feature | What it proves | Result | Success Rate | Notes |
|---------|---------------|--------|-------------|-------|
| F1: ConventionCase enum | `SnakeCase`, `CamelCase`, `PascalCase`; `from_config` parses `NamingConfig.case`; unknown values default to `SnakeCase` | PASS | 100% | 4 unit tests |
| F2: `is_valid_case` | Character-set-only validation per case; snake_case `[a-z0-9_]`, camelCase starts lowercase, PascalCase starts uppercase | PASS | 100% | 9 unit tests; structural rules separate |
| F3: `apply_case` | Lowercase token slice → formatted name for all 3 cases; empty slice → `""` | PASS | 100% | 7 unit tests including edge cases |
| F4: `to_convention_name` | Auto-fix: PascalCase → snake_case, snake_case → camelCase, snake_case → PascalCase, empty input | PASS | 100% | 4 unit tests + integration path confirmed |
| F5: `lint_catalog` — CaseViolation | Non-conforming names flagged; suggestion is correct auto-fix | PASS | 100% | 4 unit + 4 integration tests |
| F6: `lint_catalog` — ConsecutiveUnderscores | `user__signed` → ConsecutiveUnderscores, suggestion collapses `__` | PASS | 100% | 2 unit + 1 integration test |
| F7: `lint_catalog` — LeadingOrTrailingUnderscore | Leading `_` and trailing `_` both flagged; suggestion trims | PASS | 100% | 3 unit tests |
| F8: `lint_catalog` — EmptyName | Empty name → `EmptyName` violation, `suggestion = None`, no further violations | PASS | 100% | 2 unit + 1 integration test |
| F9: Multiple violations per entry | `"__BadName_"` → 3 violations (ConsecutiveUnderscores + LeadingOrTrailingUnderscore + CaseViolation) | PASS | 100% | 1 unit + 1 integration test |
| F10: Ignored entries skipped | Entries with `EventStatus::Ignored` produce zero violations regardless of name | PASS | 100% | 1 unit + 1 integration test |
| F11: Configurable case (camelCase, PascalCase) | snake_case names flagged with CaseViolation under camelCase/PascalCase config; suggestion correct | PASS | 100% | 1 unit + 2 integration tests |
| F12: NextjsAdapter end-to-end | Proposals from real Next.js parse → `from_proposals` → `lint_catalog` → 0 violations with default config | PASS | 100% | 1 integration test |
| F13: Backward compat | All 205 pre-E1.3 workspace tests still pass | PASS | 100% | `cargo test` — 248/248 total (43 new) |

## Evidence

```
$ cargo test -p infergen-core linter
cargo test: 42 passed, 186 filtered out (5 suites, 0.00s)
```

```
$ cargo test
cargo test: 248 passed (10 suites, 0.50s)
```

```
$ cargo clippy -p infergen-core
cargo clippy: No issues found
```

## N/A List

None. All E1.3 deliverables are testable without downstream dependencies.

## Recommendations

- E1.4 (Property Type Inference) and E1.5 (Review Workflow) can now consume `lint_catalog` to surface violations in interactive review UX.
- E4.2 (Drift Detection / `scan --check`) depends on this linter to fail CI on convention violations — all the building blocks are in place.
- Structural `convention` linting (entity/action/state template) was intentionally deferred. When E6.1 adds semantic name decomposition, `LintRule` can be extended with a `ConventionViolation` variant without breaking the existing API.
