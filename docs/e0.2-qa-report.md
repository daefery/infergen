# E0.2 — CLI Skeleton & Config — QA Report

| | |
|---|---|
| **Epic ID** | E0.2 |
| **Name** | CLI Skeleton & Config |
| **Source Milestone** | M0 — Foundation & Proof of Concept |
| **Date** | 2026-06-07 |
| **Branch** | `epic/e0.2-cli-skeleton-config` (pre-commit; base `c52a184`) |
| **Environment** | macOS (Darwin 25.4.0) · rustc/cargo 1.85.0 · pnpm 9.15.0 · just 1.51.0 · cargo-deny 0.19.8 |
| **Overall success rate** | **100%** (8/8 testable cases) |
| **Verdict** | 🟢 **GREEN** |

**Summary:** 8/8 cases passed (100%) · GREEN · 0 N/A.

---

## Feature results

| Feature | What it proves | Result | Success rate | Notes |
|---|---|---|---|---|
| F1 — Command tree | `infergen` exposes `init`/`scan`/`generate`/`check`/`watch` + `--version`/`--help` (ROADMAP E0.2, PRD §8) | PASS | 100% (6/6) | `--help` lists all 5 subcommands; `--version` prints `0.0.0`. |
| F2 — `init` writes config | `init` produces a valid `infergen.config.*` (PRD §5.1) | PASS | 100% (4/4) | JSON written with camelCase keys, defaults populated. |
| F3 — Config loader | discover precedence, overwrite guard, `--force`, TOML format (ROADMAP E0.2) | PASS | 100% (4/4) | refuse-without-force exits 1; `--force` exits 0; `--format toml` writes valid TOML. |
| F4 — Auto-detection | language + framework detection from manifests (ROADMAP E0.2, PRD §6.1) | PASS | 100% (3/3) | Next.js+TS, Python/FastAPI, empty-project all correct. |
| F5 — Honest stubs | `scan`/`generate`/`check`/`watch` name their owning epic | PASS | 100% (4/4) | each prints `not yet implemented — lands in E0.4/E2.1/E4.2/E4.3`. |
| F6 — No-arg banner | bare `infergen` prints status incl. config schema | PASS | 100% (2/2) | shows `core engine`, `config schema v1`. |
| F7 — Test suite | unit + integration tests green (NFR quality) | PASS | 100% | `cargo test --workspace`: 37 passed (6 suites). |
| F8 — Quality gates | clippy/fmt/deny/build green, offline (NFR §7) | PASS | 100% | `just ci` green; `cargo deny check` ok; no network in `init`. |

---

## Evidence

### F1 — Command tree
```
$ infergen --help
Usage: infergen [COMMAND]
  init      Detect languages/frameworks and write an `infergen.config.*`
  scan      Scan source and propose an event catalog. (Lands in E0.4.)
  generate  Generate a typed SDK from the catalog. (Lands in E2.1.)
  check     CI check: fail on drift / untracked moments. (Lands in E4.2.)
  watch     Watch files and re-scan on change. (Lands in E4.3.)
$ infergen --version
infergen 0.0.0
```

### F2 / F4 — `init` + detection
```
[nextjs-ts]  config languages/frameworks: "type-script" "next-js" "react"
[python]     config languages/frameworks: "python" "fast-api"
[empty]      "languages": [], "frameworks": []   (detected: no known languages/frameworks)
```

### F3 — Config loader
```
$ infergen init           # second run, existing config
Error: config already exists at ./infergen.config.json (use --force to overwrite)
exit=1
$ infergen init --force
exit=0
$ infergen init --format toml   # → infergen.config.toml
schemaVersion = 1
catalog = ".infergen/catalog.yaml"
output = "infergen/generated"
```

### F5 — Stubs
```
scan: not yet implemented — lands in E0.4
generate: not yet implemented — lands in E2.1
check: not yet implemented — lands in E4.2
watch: not yet implemented — lands in E4.3
```

### F6 — Banner
```
infergen 0.0.0
core engine 0.0.0
catalog schema v1
config schema v1
run `infergen --help` to see commands
```

### F7 / F8 — Gates
```
cargo test --workspace                            → 37 passed (6 suites)
cargo clippy --workspace --all-targets -- -D warnings → No issues found
cargo fmt --all -- --check                        → clean
cargo deny check                                  → advisories ok, bans ok, licenses ok, sources ok
just ci                                           → green (Rust + JS)
```

---

## Failures

None.

## N/A

None — all E0.2 deliverables are testable at this layer. (AST scan, codegen, watch behavior are downstream epics E0.3/E2.1/E4.3 and out of scope.)

## Recommendations

1. **Merge E0.2** — verdict GREEN, ready for PR.
2. **Next:** E0.3 (TS/JS AST parser via SWC/OXC) feeds real `scan`; E0.4 wires the Next.js adapter so the `scan` stub becomes functional.
3. Minor follow-up (non-blocking): on `init --force --format <other>`, the prior-format config file is left behind (`discover` precedence would then load the stale one). Consider warning or cleanup in a later polish pass — noted in plan risk #6.
