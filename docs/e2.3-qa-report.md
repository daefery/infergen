# E2.3 — Codegen Determinism & Safety — QA Report

## Header

| Field | Value |
|---|---|
| Epic | E2.3 |
| Name | Codegen Determinism & Safety |
| Milestone | M2 — Codegen & Type-Safe SDK |
| Date | 2026-06-07 |
| Branch | `epic/e2.3-codegen-determinism-safety` |
| Environment | macOS Darwin 25.4.0, rustc 1.96.0, cargo 1.96.0 |
| Overall success rate | **10/10 (100%)** |
| Verdict | **GREEN** |

## Summary

**10/10 cases passed (100%) · GREEN · 1 N/A (blocked by E3.x)**

All E2.3 deliverables verified. Properties sorted alphabetically within each event interface. `infergen generate --check` exits 0 when on-disk file is current, exits non-zero (with stderr message) when file is stale or missing, and never writes to disk. 393 workspace tests pass.

## Feature Results Table

| Feature | What it proves | Result | Success Rate | Notes |
|---------|---------------|--------|-------------|-------|
| F1: Property alphabetical sort (unit) | Properties within an event interface appear in ascending name order regardless of catalog insertion order | PASS | 100% | `generate_properties_sorted_alphabetically` unit test |
| F2: Property alphabetical sort (integration) | Full pipeline: catalog with out-of-order props → generated TS has them sorted | PASS | 100% | `full_pipeline_properties_sorted_alphabetically` integration test |
| F3: `--check` — up-to-date exits 0 | `infergen generate` then `infergen generate --check` → exit 0 + "up to date" | PASS | 100% | `generate_check_up_to_date_exits_zero` CLI test |
| F4: `--check` — missing file exits 1 | `infergen generate --check` with no prior file → exit 1 + "stale" in stderr | PASS | 100% | `generate_check_missing_file_exits_nonzero` CLI test |
| F5: `--check` — stale file exits 1 | Generate, tamper content, `--check` → exit 1 + "stale" in stderr | PASS | 100% | `generate_check_stale_file_exits_nonzero` CLI test |
| F6: `--check` — does not write | `--check` on stale file must not update the file | PASS | 100% | `generate_check_does_not_write_file` CLI test — sentinel content preserved |
| F7: Deterministic output (existing) | Same catalog → byte-identical output across runs | PASS | 100% | `generate_deterministic` unit + `full_pipeline_output_contains_no_current_year` integration |
| F8: No timestamp in header | Header contains version only, no date/time | PASS | 100% | `full_pipeline_output_contains_no_current_year` (still passing) |
| F9: Event ordering (existing) | Events sorted alphabetically by name | PASS | 100% | `generate_events_sorted_alphabetically` unit + integration |
| F10: Full workspace green | No regressions in 393 tests | PASS | 100% | `cargo test`: 393 passed (13 suites) |

## N/A Features

| Feature | Blocking Epic |
|---------|--------------|
| `tsc --noEmit` golden-file type-check of generated output | E3.1 / E2.3 ext — requires a TypeScript toolchain in CI |

## Evidence

```
$ cargo test -p infergen-core codegen
cargo test: 30 passed, 328 filtered out (8 suites, 0.00s)

$ cargo test -p infergen-core --test codegen
cargo test: 11 passed (1 suite, 0.00s)

$ cargo test -p infergen-cli
cargo test: 25 passed (2 suites, 0.32s)

$ cargo test
cargo test: 393 passed (13 suites, 0.42s)

$ cargo clippy
cargo clippy: No issues found
```

## Recommendations

- E4.2 (`infergen check`) can reuse the same "generate in memory + compare to disk" pattern at a broader scope (catalog drift, untracked moments).
- A future CI recipe doc (E4.4) should show `infergen generate --check` as the step that fails the build when the generated SDK is stale.
