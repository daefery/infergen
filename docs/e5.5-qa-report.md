# QA Report — E5.5 Monorepo / Polyglot Catalog

**Epic:** E5.5  
**Name:** Monorepo / Polyglot Catalog  
**Milestone:** M5 — Polyglot & Framework Breadth  
**Date:** 2026-06-08  
**Branch:** `epic/e5.5-monorepo-polyglot-catalog`  
**Environment:** macOS Darwin 25.4.0 · Rust (cargo test)  
**Overall success rate:** 16/16 (100%)  
**Verdict:** GREEN

---

## Summary

**16/16 cases passed (100%) · GREEN · 0 N/A**

---

## Feature Results

| Feature | What it proves | Result | Success Rate | Notes |
|---------|---------------|--------|-------------|-------|
| `detect_monorepo` — npm workspaces | Detects `packages/*` glob in `package.json` | PASS | 100% | 2 packages named from `package.json#name` |
| `detect_monorepo` — pnpm workspace | Detects `pnpm-workspace.yaml` `packages:` field | PASS | 100% | 2 packages correctly named |
| `detect_monorepo` — Cargo workspace | Detects `[workspace] members` in root `Cargo.toml` | PASS | 100% | Package names from `[package] name` |
| `detect_monorepo` — multi-manifest fallback | Detects subdirs each with a manifest when no explicit workspace config | PASS | 100% | Scans immediate children only |
| `detect_monorepo` — single package | Root with no workspace config → empty packages | PASS | 100% | `is_monorepo()` returns false |
| `MonorepoLayout.is_monorepo()` | Returns `true` iff > 1 package | PASS | 100% | Tested for 0 and 2 packages |
| `MonorepoLayout.language_count()` | Counts distinct languages across all packages | PASS | 100% | Dedup: 2 TS pkgs → 1; TS+Python → 2 |
| `namespace_catalog` | Sets `package` field on all entries; original unchanged | PASS | 100% | Tested empty catalog too |
| `merge_package_catalogs` — two packages | Merges, namespaces, event count correct | PASS | 100% | All entries have package field |
| `merge_package_catalogs` — 3×4 events | 12 events total, all namespaced | PASS | 100% | |
| `merge_package_catalogs` — sort order | Sorted by (package\_name, id) | PASS | 100% | "alpha" before "backend" |
| `merge_package_catalogs` — dedup | Same ID within package deduped | PASS | 100% | |
| `cross_service_check` — clean | Distinct event names → no issues | PASS | 100% | |
| `cross_service_check` — PropertyConflict | Same name, different props → conflict | PASS | 100% | `kind == PropertyConflict` |
| `CatalogEntry.package` — YAML round-trip | Serializes and deserializes correctly | PASS | 100% | `package = Some("frontend")` preserved |
| `CatalogEntry.package` — backward compat | `package = None` → field absent from YAML | PASS | 100% | Existing catalogs unaffected |

---

## Test Evidence

```
cargo test -p infergen-types -p infergen-core
956 passed, 1 ignored (17 suites, 0.17s)

cargo test -p infergen-core monorepo
29 passed, 911 filtered out (14 suites, 0.01s)
```

Unit tests in `monorepo.rs`: 23 (including all 5 `detect_monorepo` variants, `is_monorepo`, `language_count`, `namespace_catalog`, `merge_package_catalogs`, `cross_service_check`)

Integration tests in `tests/monorepo.rs`: 12 (real tempdir file trees)

---

## Regressions

No regressions. 956 pre-existing tests pass (was 927 before E5.5; net +29 new tests).

---

## Recommendations

None — all cases GREEN.
