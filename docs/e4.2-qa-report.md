# QA Report — E4.2 Drift Detection (`scan --check`)

**Epic:** E4.2  
**Name:** Drift Detection (`scan --check`)  
**Milestone:** 4 — Incremental Scans & CI  
**Date:** 2026-06-08  
**Branch:** `epic/e4.2-drift-detection-scan-check`  
**Commit:** `0e1bc94`  
**Environment:** macOS Darwin 25.4.0 · cargo 1.96.0  
**Overall success rate:** 6/6 cases passed (100%)  
**Verdict:** GREEN

---

## Summary

**6/6 cases passed (100%) · GREEN · 0 N/A**

---

## Feature Results Table

| Feature | What it proves | Result | Success Rate | Notes |
|---------|---------------|--------|-------------|-------|
| No catalog → fail with hint | PRD §5 step 7 / §6.5: CI requires existing catalog | PASS | 100% | Exit 1, stderr "run `infergen scan` first" |
| Clean approved catalog → pass | PRD §6.5: no drift = OK | PASS | 100% | Exit 0, stdout "check: OK" |
| Proposed events → fail (unreviewed tier) | PRD §6.5: unreviewed events flagged | PASS | 100% | Exit 1, stdout "unreviewed event(s)" with `?` marker |
| New scan event not in catalog → fail (untracked tier) | PRD §5/§6.5: untracked moments flagged | PASS | 100% | Exit 1, stdout "new untracked moment(s)" with `+` marker |
| Convention violation → fail | PRD §6.5: convention violations flagged | PASS | 100% | Exit 1, stdout "violation(s)" with auto-fix suggestion |
| `--catalog` flag overrides default path | Config flexibility | PASS | 100% | Custom yaml path read, OK when clean |

---

## Evidence

**F1 — No catalog:**
```
$ infergen check
Error: check: catalog not found at .infergen/catalog.yaml — run `infergen scan` first
exit: 1
```

**F2 — Clean approved catalog:**
```
$ infergen check
check: OK — catalog is clean
exit: 0
```

**F3 — Proposed event:**
```
$ infergen check
check: 1 unreviewed event(s) — run `infergen review` to approve or ignore:
  ? page_viewed (evt_abc0000000000002)
Error: check failed: 1 issue(s) found
exit: 1
```

**F4 — Convention violation:**
```
$ infergen check
check: 1 naming convention violation(s):
  ! pageViewed → page_viewed
Error: check failed: 1 issue(s) found
exit: 1
```

**F5 — New untracked moment:**
```
$ infergen check
check: 1 new untracked moment(s) — run `infergen scan` to record:
  + home_page_viewed (evt_7d9353257349791b)
Error: check failed: 1 issue(s) found
exit: 1
```

**F6 — Custom catalog path:**
```
$ infergen check --catalog custom.yaml
check: OK — catalog is clean
exit: 0
```

---

## Test Suite Results

```
infergen-cli:   37 passed, 0 failed  (incl. 7 check integration tests)
infergen-core: 350 passed, 0 failed
  integration:  35 passed, 0 failed
  codegen:      24 passed, 0 failed
  catalog:      35 passed, 0 failed
infergen-types:  8 passed, 0 failed
doc-tests:       2 passed, 0 failed
TOTAL:         491 tests, 0 failures
```

---

## N/A List

- **Stale generated code check**: Already implemented by `infergen generate --check` (E2.3). Scoped out of E4.2 to avoid duplication. CI pipelines pair `infergen check` with `infergen generate --check`.

---

## Recommendations

All tests GREEN. Proceed to E4.3 (Watch Mode) which depends on E4.1.
