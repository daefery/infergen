# E2.1 — TypeScript Codegen — QA Report

## Header

| Field | Value |
|---|---|
| Epic | E2.1 |
| Name | TypeScript Codegen |
| Milestone | M2 — Codegen & Type-Safe SDK |
| Date | 2026-06-07 |
| Branch | `epic/e2.2-generated-sdk-ergonomics` |
| Environment | macOS Darwin 25.4.0, rustc 1.96.0, cargo 1.96.0 |
| Overall success rate | **9/9 (100%)** |
| Verdict | **GREEN** |

> **Note:** E2.1 was co-implemented with E2.2 in PR #11. This report covers E2.1 deliverables only. See `e2.2-qa-report.md` for the combined run including E2.2 ergonomics features.

## Summary

**9/9 cases passed (100%) · GREEN · 2 N/A (blocked by E3.x)**

Core TypeScript codegen verified: typed interfaces per event, `EventName` union, named `trackXxx` functions, `track` namespace object, approved-only filter, deterministic/idempotent output. 387 workspace tests pass.

## Feature Results Table

| Feature | What it proves | Result | Success Rate | Notes |
|---------|---------------|--------|-------------|-------|
| F1: `generate_typescript` — empty catalog | `EventName = never`, empty `track = {} as const`, no crash | PASS | 100% | Unit + integration tests |
| F2: Typed `XxxProperties` interface | One interface per approved event; correct TS types (string/number/boolean/unknown for None) | PASS | 100% | 5 unit + 2 integration tests |
| F3: Named `trackXxx` export functions | One `export function trackXxx(properties: XxxProperties): void` per event | PASS | 100% | Unit + integration tests |
| F4: `track` namespace object | All approved events as camelCase keys, `as const` | PASS | 100% | 2 unit + 1 integration test |
| F5: `EventName` union type | All approved event names listed; semicolon on last entry only | PASS | 100% | 2 unit + 1 integration test |
| F6: Approved-only by default | Ignored and Proposed events excluded; `include_proposed: true` re-includes Proposed | PASS | 100% | 4 unit + 2 integration tests |
| F7: Deterministic / idempotent output | Same catalog → same output every run; no timestamps | PASS | 100% | `generate_deterministic` unit + `full_pipeline_output_contains_no_current_year` integration |
| F8: Alphabetical event ordering | Events sorted by name in all output sections | PASS | 100% | 1 unit + implicit in integration |
| F9: Name conversion helpers | `to_pascal_case` and `to_camel_case` correct for snake_case, single-word, single-char | PASS | 100% | 6 unit tests |

## N/A Features

| Feature | Blocking Epic |
|---------|--------------|
| Generated functions fire real events to providers | E3.1 (Provider Plugin Interface) |
| Tree-shaking verified with bundler | E2.3 (Codegen Determinism & Safety) |

## Evidence

```
$ cargo test -p infergen-core codegen
cargo test: 29 passed, 358 filtered out (8 suites, 0.00s)

$ cargo test -p infergen-core --test codegen
cargo test: 10 passed (1 suite, 0.00s)

$ cargo test
cargo test: 387 passed (13 suites, 0.48s)

$ cargo clippy
cargo clippy: No issues found
```

## Recommendations

- E2.3 should add a golden-file test: write the generated `.ts` to disk, run `tsc --noEmit`, verify TypeScript itself accepts the output.
- E3.1 will replace the `throw new Error(...)` stub in generated functions with real provider dispatch.
